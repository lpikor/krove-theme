{
  "name": "krove",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "npx node-sass --output-style compressed assets/css -o assets/css",
    "build": "npm run scss",
    "watch": "npx onchange \"assets/css/*.scss\" -- npm run build",
    "dev": "npm run watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^8.0.0",
    "onchange": "^7.1.0"
  }
}
